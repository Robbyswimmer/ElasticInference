apiVersion: v1
kind: Service
metadata:
  name: gateway-headless
  namespace: llm-inference
spec:
  clusterIP: None
  selector:
    app: gateway
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
