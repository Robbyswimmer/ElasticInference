apiVersion: apps/v1
kind: Deployment
metadata:
  name: decode
  namespace: llm-inference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: decode
  template:
    metadata:
      labels:
        app: decode
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
    spec:
      containers:
        - name: decode
          image: llm-inference/worker:latest
          imagePullPolicy: Never
          command: ["python3", "-m", "workers.decode"]
          ports:
            - containerPort: 50053
              name: grpc
            - containerPort: 9090
              name: metrics
          env:
            - name: REDIS_HOST
              value: "redis"
            - name: CUDA_MPS_ACTIVE_THREAD_PERCENTAGE
              value: "100"
          readinessProbe:
            exec:
              command:
                - python3
                - -c
                - "import grpc; ch=grpc.insecure_channel('localhost:50053'); grpc.channel_ready_future(ch).result(timeout=2)"
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            exec:
              command:
                - python3
                - -c
                - "import grpc; ch=grpc.insecure_channel('localhost:50053'); grpc.channel_ready_future(ch).result(timeout=2)"
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
          resources:
            requests:
              cpu: "1"
              memory: "4Gi"
              nvidia.com/gpu: "1"
            limits:
              cpu: "4"
              memory: "8Gi"
              nvidia.com/gpu: "1"
