apiVersion: v1
kind: Service
metadata:
  name: decode-headless
  namespace: llm-inference
spec:
  clusterIP: None
  selector:
    app: decode
  ports:
    - name: grpc
      port: 50053
      targetPort: 50053
