apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: llm-inference
spec:
  type: NodePort
  selector:
    app: gateway
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
      nodePort: 30051
    - name: metrics
      port: 9090
      targetPort: 9090
