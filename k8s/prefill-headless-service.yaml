apiVersion: v1
kind: Service
metadata:
  name: prefill-headless
  namespace: llm-inference
spec:
  clusterIP: None
  selector:
    app: prefill
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
